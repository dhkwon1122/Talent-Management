"""
인재 역량 대시보드 메인 진입점.
사이드바에서 페이지를 선택하거나 Streamlit 멀티페이지 네비게이션을 사용한다.
"""
import sys
from pathlib import Path

ROOT = Path(__file__).parent
sys.path.insert(0, str(ROOT))

import streamlit as st

st.set_page_config(
    page_title="인재 역량 대시보드",
    page_icon="🔬",
    layout="wide",
    initial_sidebar_state="expanded",
)

st.title("🔬 연구소 인재 역량 대시보드")
st.markdown("리더급 후보 인재의 **6개 역량 차원**을 정량화하여 시각화합니다.")

st.markdown(
    """
    | 차원 | 구성 지표 (가중치) |
    |------|--------------------|
    | 전문성 | 논문 수 (30%) · 피인용 수 (30%) · h-index (20%) · 등록특허 (20%) |
    | **업무성과** | **연도별 평가등급 가~마 (50%)** · KPI 달성률 (25%) · 주관과제 수 (15%) · 관리예산 (10%) |
    | 리더십 | 리더십 코멘트 LLM (30%) · 멘토링 건수 (25%) · 과제 리더 경험 (25%) · 동료 평가 (20%) |
    | 글로벌역량 | 국제 공동 논문 (40%) · 어학 점수 (30%) · 해외 파견 기간 (30%) |
    | 협업네트워크 | 동료평가 코멘트 LLM (40%) · 사내 협업 과제 (25%) · 외부기관 협력 (25%) · 동료 평가 (10%) |
    | 성장잠재력 | KPI 성장 추이 (50%) · 연평균 교육 시간 (30%) · 성취 효율성 (20%) |
    """
)

with st.expander("📐 점수 계산 방식 상세 보기"):
    st.markdown(
        """
        ### 공통 파이프라인

        ```
        Raw CSV → 피처 집계 → 차원별 가중합(원점수) → Min-Max 정규화 + √변환 → 0~100점
        ```

        모든 차원은 동일한 정규화 공식을 적용합니다.

        $$\\text{정규화 점수} = \\sqrt{\\frac{x - x_{min}}{x_{max} - x_{min}}} \\times 100$$

        > 루트 변환을 통해 하위 점수대의 가독성을 높이고, 최상위 집중 현상을 완화합니다.

        ---

        ### 업무성과 — 평가등급 점수화 로직

        연도별 인사평가 등급을 수치로 환산한 뒤, **최근 연도에 높은 가중치**를 부여해 합산합니다.

        | 등급 | 환산 점수 |
        |------|-----------|
        | 가   | 100점 |
        | 나   | 80점  |
        | 다   | 60점  |
        | 라   | 40점  |
        | 마   | 20점  |

        **가중 평균 공식** (n년치 데이터, 오래된 순 1·2·…·n 선형 가중치)

        $$\\text{grade\\_score} = \\frac{\\sum_{i=1}^{n} i \\times \\text{보정점수}_i}{\\sum_{i=1}^{n} i}$$

        예) 3년 데이터 [다, 나, 가] → (1×60 + 2×80 + 3×100) / 6 = **86.7점**

        ---

        ### 업무성과 — 연봉 조정율(성과인상율)에 따른 점수 보정

        같은 등급이라도 **해당 연도·직급·등급의 기준 인상율 대비 개인이 실제 받은 인상율**이
        높을수록 등급 점수를 상향 보정하고, 낮을수록 하향 보정합니다.

        **보정 계수 산출**

        $$\\text{보정계수} = \\text{clamp}\\!\\left(\\frac{\\text{개인 성과인상율}}{\\text{기준 인상율}_{(\\text{연도, 직급, 등급})}},\\; 0.7,\\; 1.3\\right)$$

        **보정 점수 적용**

        $$\\text{보정점수}_i = \\text{등급 환산점수}_i \\times \\text{보정계수}_i$$

        | 항목 | 설명 |
        |------|------|
        | 기준 인상율 | `data/raw/merit_standards.csv` — 연도·직급·등급별 기준율 |
        | 보정 계수 범위 | 0.7 ~ 1.3 (기준 대비 ±30% 초과분은 클램프) |
        | 적용 단위 | 연도별 개별 적용 후 가중 평균 산출 |

        **기준 인상율 구조** (`merit_standards.csv`)

        ```
        year | position  | grade | base_rate
        2024 | 책임연구원 | 가    | 0.0461
        2024 | 선임연구원 | 가    | 0.0521
        2024 | 수석연구원 | 가    | 0.0581
        2024 | 수석연구원 | 다    | 0.0348
        ...
        ```

        > 실제 운영 시 이 CSV를 회사 기준 데이터로 교체하면 시스템 코드 변경 없이 반영됩니다.

        **계산 예시**

        수석연구원 A가 2024년 "나" 등급을 받고 개인 인상율 5.5%, 해당 연도 수석·나 기준율 5.2% 일 때

        $$\\text{보정계수} = \\text{clamp}(5.5 / 5.2,\\; 0.7,\\; 1.3) = 1.058$$
        $$\\text{보정점수} = 80 \\times 1.058 = \\mathbf{84.6점}$$

        최종 업무성과 원점수 = `grade_score × 0.50 + KPI × 0.25 + 주관과제 × 0.15 + 예산 × 0.10`

        ---

        ### 리더십 · 협업네트워크 — LLM 텍스트 분석

        동료/리더십 코멘트를 **Google Gemini API**로 분석해 0~10점 산출.
        API 키 미설정 시 키워드 기반 로컬 점수로 자동 대체되며,
        결과는 `data/processed/text_scores.json`에 캐싱됩니다.

        ---

        ### 성장잠재력 — KPI 성장률

        최근 3년 평균 KPI vs. 이전 기간 평균 KPI의 **증감률(%)**을 사용합니다.

        $$\\text{growth\\_rate} = \\frac{\\text{KPI}_{\\text{최근 3년}} - \\text{KPI}_{\\text{이전}}}{\\text{KPI}_{\\text{이전}}}$$
        """
    )

st.markdown("---")
st.caption("왼쪽 사이드바에서 원하는 페이지를 선택하세요.")
